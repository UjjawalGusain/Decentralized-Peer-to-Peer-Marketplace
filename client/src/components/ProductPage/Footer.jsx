import React from "react";

const Footer = () => (
  <footer className="mt-12 text-center text-sm text-gray-500">
    © {new Date().getFullYear()} GoCart Replica — demo UI only.
  </footer>
);

export default Footer;
